import React from 'react'

const TitlePage = ({ title }) => {
    return (
        <div className='bg-gradient-to-tr bg-orange-500 to-red-500
    uppercase text-center py-2'>{title}</div>
    )
}

export default TitlePage